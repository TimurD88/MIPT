#include <stdio.h>
#include <math.h>
#include "func.h"
#include "test.h"


// #ifdef TESTING
// ... 
// #endif
int main()
{
    double x1 = NAN, x2 = NAN;
    double a = NAN, b = NAN, c = NAN;
	test_unit(); // naming
	/*
    while (1)
    { 
    	a = get_num("enter a:");
    	b=get_num("enter b:");
    	c=get_num("enter c:");
    	printf("a = %g  b= %g c= %g\n", a, b, c);
    	if ( !isnan(a) && !isnan(b) && !isnan(c) ) // in getnum
    		break;
    }
	*/
	
	/*
    int nRoots = eq_solver(a, b, c, &x1, &x2);
    num_print(nRoots);
*/
    return 0; 
}













